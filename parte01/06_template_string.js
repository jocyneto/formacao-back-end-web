/**
 * - Template string nada mais é que a construção de string de forma elegante e prática.]
 * - Obrigatoriamente usamos o acento grave ``
 * - Para usarmos variaveis usamos a seguinte estrutura ${}
 * Exemplo:
 */

let nome = "Jocy"
let idade = 28;

console.log(`Meu nome é ${nome} e minha idade é ${idade}.`);